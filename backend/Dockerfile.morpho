FROM python:3.11-slim

WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn pycryptodome

COPY backend/morpho/ /app/morpho/

ENV PYTHONPATH=/app
ENV PYTHONUNBUFFERED=1

EXPOSE 8080
CMD ["uvicorn", "morpho.indexer:app", "--host", "0.0.0.0", "--port", "8080"]
