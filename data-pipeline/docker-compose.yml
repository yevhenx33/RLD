version: '3.8'

services:
  graphql_api:
    image: rld_indexer_node
    container_name: rld_graphql_api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - CLICKHOUSE_HOST=172.17.0.1
      - CLICKHOUSE_PORT=8123
    command: ["uvicorn", "indexer.api.graphql:create_app", "--factory", "--host", "0.0.0.0", "--port", "5000", "--workers", "2", "--reload"]
    volumes:
      - ./indexer:/app/indexer
    ports:
      - "127.0.0.1:5000:5000"

  aave_collector:
    image: rld_indexer_node
    container_name: rld_aave_collector
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - CLICKHOUSE_HOST=172.17.0.1
      - CLICKHOUSE_PORT=8123
      - SEND_MESSAGES=true
    command: ["python", "/app/scripts/run_indexer.py", "--source", "AAVE_MARKET", "--role", "collector"]
    network_mode: "host"

  aave_processor:
    image: rld_indexer_node
    container_name: rld_aave_processor
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - CLICKHOUSE_HOST=172.17.0.1
      - CLICKHOUSE_PORT=8123
      - SEND_MESSAGES=true
    command: ["python", "/app/scripts/run_indexer.py", "--source", "AAVE_MARKET", "--role", "processor"]
    network_mode: "host"

  morpho_collector:
    image: rld_indexer_node
    container_name: rld_morpho_collector
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - CLICKHOUSE_HOST=172.17.0.1
      - CLICKHOUSE_PORT=8123
      - SEND_MESSAGES=true
    command: ["python", "/app/scripts/run_indexer.py", "--source", "MORPHO_MARKET", "--role", "collector"]
    network_mode: "host"

  morpho_processor:
    image: rld_indexer_node
    container_name: rld_morpho_processor
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - CLICKHOUSE_HOST=172.17.0.1
      - CLICKHOUSE_PORT=8123
      - SEND_MESSAGES=true
    command: ["python", "/app/scripts/run_indexer.py", "--source", "MORPHO_MARKET", "--role", "processor"]
    network_mode: "host"

  sofr_collector:
    image: rld_indexer_node
    container_name: rld_sofr_collector
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - CLICKHOUSE_HOST=172.17.0.1
      - CLICKHOUSE_PORT=8123
    command: ["python", "/app/scripts/run_indexer.py", "--source", "SOFR_RATES", "--role", "collector"]
    network_mode: "host"

  chainlink_collector:
    image: rld_indexer_node
    container_name: rld_chainlink_collector
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - CLICKHOUSE_HOST=172.17.0.1
      - CLICKHOUSE_PORT=8123
    command: ["python", "/app/scripts/run_indexer.py", "--source", "CHAINLINK_PRICES", "--role", "collector"]
    network_mode: "host"

  chainlink_processor:
    image: rld_indexer_node
    container_name: rld_chainlink_processor
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - CLICKHOUSE_HOST=172.17.0.1
      - CLICKHOUSE_PORT=8123
    command: ["python", "/app/scripts/run_indexer.py", "--source", "CHAINLINK_PRICES", "--role", "processor"]
    network_mode: "host"
